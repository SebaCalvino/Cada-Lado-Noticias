export default function Footer() {
  return (
    <footer className="bg-white border-t border-gray-200 mt-16">
      <div className="max-w-6xl mx-auto px-4 py-8 flex flex-col sm:flex-row items-center justify-between gap-4 text-sm text-gray-500">
        <p>
          <span className="font-semibold text-gray-700">Cada Lado Noticias</span> — La misma noticia, todas las voces.
        </p>
        <p>Síntesis generada con IA · Fuentes verificadas · Argentina</p>
      </div>
    </footer>
  )
}
